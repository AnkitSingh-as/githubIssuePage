import React from 'react'


// This is a loading component, which can be modified later to improve the UI.

const Loading = () => {
  return (
    <div
        style={{ display: 'flex', justifyContent: 'center', alignItems: 'center' , padding: '250px'}}
    >
        Loading...
    </div>
  )
}

export default Loading